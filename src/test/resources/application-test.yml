spring:
  datasource:
    url: jdbc:mysql://localhost:3306/temp_database?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

  data:
    redis:
      host: localhost
      port: 6379

aws:
  s3:
    bucket: test-bucket

file:
  upload-path: uploads